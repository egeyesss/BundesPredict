# FastAPI service. Build context is the repo root (needs src/ + apps/api/).
FROM python:3.12-slim

WORKDIR /app

# Install deps first for layer caching, then the source.
COPY pyproject.toml ./
COPY src ./src
COPY apps/api ./apps/api
# The API serves the fitted model and the agent, so it needs both extras.
RUN pip install --no-cache-dir -e ".[model,agent]"

EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
